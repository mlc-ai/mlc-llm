# QAT Training Requirements
# Core ML/DL libraries
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.21.0
peft>=0.6.0
bitsandbytes>=0.41.0
safetensors>=0.4.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Visualization and logging
matplotlib>=3.7.0
tqdm>=4.65.0

# Configuration
pyyaml>=6.0

# Optional: Weights & Biases for experiment tracking
# wandb>=0.15.0

# Optional: TensorBoard for logging
# tensorboard>=2.13.0

# Note: The actual MLC-LLM package is installed from the parent directory
# This requirements.txt is specifically for the QAT training components